# Codename Factory

## Квестовая система написанная с помощью Blueprints на Unreal Engine 5.6 

## 🎯 Ключевые Реализованные Системы

Гибкая и расширяемая система для создания квестов :
### 1. Гибкая Квестовая Система

 Модульная система квестов, спроектированная с упором на расширяемость:
 * **Управление данными через DataTable** - Создание квеста происходит в DataTable, по шаблону Квест -> Этапы квеста -> Задачи конкретного этапа. 
 * **Гибкость создания квеста** - обширное количество параметров для указания Типа квеста, вида выполняемых задач, количество наград, опциональные задачи и так далее.
 * **Независимость от системы** - Каждый квест является отдельной сущностью и самостоятельно отслеживает выполнение задач и этапов;
 * **Actor Components(AC)** - активное использование AC для создания, выдачи и продвижения по квесту; 

### 2. Событийно-Ориентированная архитектура

Взаимодействие между системами построено на принципах слабой связи - избегаем Hard References и чётко делим ответственность:
* **Диспатчеры(делегаты)** - все события(например, "Получен Новый Квест!", "Прогресс по квесту Обновлён", "Этап Выполнен", "Задача Выполнена",) транслируются через Диспатчеры. Затем другие системы, например UI, подписываются на эти события, чтобы обновить своё состояние.
* **Blueprint Интерфейсы** - являются частью компонентов, благодаря этому взаимодействие игрока с любым предметом/ассетом/инструментом, будет также продвигать квест, если объект являлся его частью.

### 3. Модульная Система UI

Пользовательский интерфейс спроектирован как композит из разных виджетов, это позволяет избежать сложностей с отображением, позволяет легко добавить или изменить функционал отдельной конкретной части без "копания" внутри архитектуры:
* **Журнал Квестов** - является самым обширным и комплексным, потому что является композицией виджетов: панель названий квестов, панель описания квеста, панель текущих задач, кнопки и т.д. ;
* **HUD** - небольшое окошко в левой части экрана для отслеживания текущего квеста и задач текущего этапа, а также являются ли поставленные задачи опциональными;
* **Отсутствие количественное привязки** - паттерн "композиция" позволяет обеспечить максимальную гибкость в отображении любых деталей квеста, в любом(разумном) количестве;
* **Разделение Данных и Отображения** - виджеты не хранят в себе данные и логику квестов. Их задача - отобразить содержание структуры квеста. Помимо оптимизации данных подход обеспечивает лёгкость добавления функционала или изменения, а также облегчает дебаггинг ;

## 🛠️ Подробности реализованных систем
### Какие параметры есть у квеста, этапа, задач ?

* **Cоздания квеста** - используется Enum для выбора типа квеста: 
	 1. Главный, 
	 2. Побочный, 
	 3. Временный (Event, ограниченное по времени событие), 
	 4. Ежедневный. 

* **Создание стадии** - каждый квест имеет множество стадий, за каждую стадию есть отдельная награда, награда выполнена в виде массива структур, структура имеет два параметра - **Data Asset** и количество. Данная структура закладывает возможность легкого интегрирования с **системой инвентаря** при необходимости

* **Создание задачи** - задачи могут быть **опциональными**. Опциональные задачи не влияют на успешность выполнения квеста. За каждую стадию выдаётся множество наград. Существует 4 типа задач: 
	1. Собрать(количественный);
	2.  Дойти до локации;
	3.  Уничтожить(количественный);
	4.  Взаимодействовать.
